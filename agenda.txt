Agenda
1) Setup the github{repository}
    a) new environment
    b) setup.py file- setup.py is a Python file, the presence of which is an indication that the module/package 
        you are about to install has likely been packaged and distributed with Distutils, which is the standard for 
        distributing Python Modules.
    c) requirements.txt
2) create src and build packages- now this is where the main part of the code will be present here
3) in src we create a components folder, which has codes for our project like data ingestion, transformation(OHE,etc)
4) we create a pipeline folder in src, where we have 2 types
    training pipeline 
    prediction pipeline
5) we create logger and exception files and utils-to push model on mongo
6) write code for logger and exception
7) we do data ingestion now
8) then we do data_tranformation simultaneuously editing the utils.py file as well in each of the three steps 
9) then we do the model training part and mak eit such that on runnng data_ingestion all the other 2 files also run
10) we do hyperparameter tuning part 
11) now we implement the prediction pipeline
12) here we create a webapp to put in input values for data and then make predictions
13) we do deployment


AWS ECR using EC2 instance
1) Docker build is checked
2) Github Workflow 
3) IAM user creation in AWS
4) CREATE an ECR ie a repository
5) EC2 instance creation in the ECR repository
6) docker setup in EC2 instance
            ## Docker Setup In EC2 commands to be Executed

            #optinal

            sudo apt-get update -y

            sudo apt-get upgrade

            #required

            curl -fsSL https://get.docker.com -o get-docker.sh

            sudo sh get-docker.sh

            sudo usermod -aG docker ubuntu

            newgrp docker

